{
  "name": "wallet-codesabai-landing",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "../node_modules/.bin/nuxt dev --host 0.0.0.0 --port 4173",
    "build": "npx nuxi generate && node ./scripts/copy-static.mjs",
    "preview": "python3 -m http.server 4173 -d dist"
  }
}
