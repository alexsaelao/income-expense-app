{
  "name": "wallet-codesabai",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "TMPDIR=/tmp nuxt dev --host 0.0.0.0",
    "dev:landing": "npm --prefix landing run dev",
    "build": "nuxt build",
    "build:landing": "npm --prefix landing run build",
    "generate": "nuxt generate",
    "generate:landing": "npm --prefix landing run generate",
    "preview": "nuxt preview",
    "preview:landing": "npm --prefix landing run preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@libsql/client": "^0.17.3",
    "@nuxt/ui": "^3.3.3",
    "@vite-pwa/nuxt": "^1.0.4",
    "nuxt": "^4.1.1"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vue-tsc": "^2.2.10"
  }
}
